#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(0)
{
	char i;

	for (i = 'a' ; i <= 'z' i++)
		if (i != q && i != e)
			putchar(i);
	putchar('\n');
	return (0);
}
